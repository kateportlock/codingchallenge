import styles from "../../styles/modules/Header.module.scss";

const Header = (props) => {

    return (
        <div className={styles.header}>
            <p>Police Forces in the UK</p>
        </div>
    )
}

export default Header;